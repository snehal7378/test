/*
#include<stdio.h>
int main()
{
    int actualprice=0 , sellingprice=0 , profit=0 , loss=0 ;


    printf("Enter actual price:");
    scanf("%d",&actualprice);

    printf("Enter the selling price:");
    scanf("%d",&sellingprice);

       if(sellingprice>actualprice);
         {
            profit=sellingprice-actualprice;
            printf("\n Profit=%d",profit);
         }
         else (actualprice==sellingprice);
         { 
            printf("there is no profit no loss:");
         }
        
         else (sellingprice<actualprice)

        {
            loss=actualprice-sellingprice;
            printf("\n loss=%d",loss);
        }
        else
        {
            printf("there is no pofit:")
        }

        printf("\n---------Thank You! visit again! !-------");

        return 0;

}*/

