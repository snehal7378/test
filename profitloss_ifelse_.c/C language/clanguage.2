#include<stdio.h>
int main()
{
   int a = 10 , b = 20;

     // if (a == 10 || b == 10)// at least one of them should be true 
     //{
     //    printf("\nEqual\n");
     //}
     // else
     //{
     //  printf("\nNot Equal\n");
     //}

     // if (a == 10) // a value should be equal to 10 then result will be 
     //{
        printf("\nEqual\n");
     //}
     //else
     //{
        printf("\nnot Equal\n");
     //}

      a = 10;
      if (a != 10)// a value should not be equal to 10 then result will be true
      {
         printf("\n True\n");
         
      }
       else
       {
         printf("\n False \n");
       }

       /*
       int n;
       printf("\n enter the value of n: =");
       scanf("%d",&n);

       // if else statment 

       if  (n % 2 == 0)
       {
         printf("\n Even Number ...!\n");
       }
        else 
      {
         printf("\n Odd Number ...!\n");
      }

      */


return 0;
}
