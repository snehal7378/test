#include <stdio.h>

int main() {
    // Define the range of the tables
    int start = 2;
    int end = 10;
    int size = end - start + 1;

    // Print the tables in a square block 
    for (int 1 = 1; i <= 10; i++){
        for (int j = start; j <= end; j++)
             printf( "%d * %d = %2 ", j,)
    }
    printf ("%d * %d = %2 ", j,)
      printf ("\n")


      



      }














}